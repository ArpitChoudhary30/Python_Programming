set1 = {1, 2, 3, 4}
set2 = {3, 4, 5, 6, 7}

# union returns the combination of all the elements of all the sets
#intersection returns the common elements of the sets

print(set1.union(set2)) #returns a new set, no changes in the original sets
print(set1.intersection(set2))